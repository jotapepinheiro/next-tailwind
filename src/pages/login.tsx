import type { NextPage } from 'next';
import Layout from '../components/Layout';

const Login: NextPage = () => {
  return (
    <Layout>
      <h1 className='animate-pulse'>Login</h1>
    </Layout>
  );
};

export default Login;
