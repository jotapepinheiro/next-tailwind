import type { NextPage } from 'next';
import Layout from '../../components/Layout';

const Raffles: NextPage = () => {
  return (
    <Layout>
      <h1 className='animate-pulse'>Lista de Sorteios</h1>
    </Layout>
  );
};

export default Raffles;
